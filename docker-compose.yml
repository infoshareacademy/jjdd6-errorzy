version: "2"
services:
  db:
    image: mysql
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=errorzy_datavase
      - MYSQL_USER=root
      - MYSQL_PASSWORD=root
    ports:
      - 6603:3306
    command: mysqld --character-set-server=utf8 --collation-server=utf8_unicode_ci --init-connect='SET NAMES UTF8;' --innodb-flush-log-at-trx-commit=0
  app:
    build:
      context: ./
    ports:
      - 8080:8080
      - 9990:9990
    depends_on:
      - db
    links:
      - db
    dns: 8.8.8.8
